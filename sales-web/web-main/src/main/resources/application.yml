spring:
  application:
    name: sales-web
  profiles:
    active: dev
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
server:
  servlet:
    context-path: /
  port: 10580

nicico:
  oauth2:
    server:
      url: http://172.16.180.22
  welcome:
    url: /salesMainDesktop
  upload:
    dir: c:\upload

---

spring:
  profiles: dev
  security:
    oauth2:
      client:
        registration:
          sso-login:
            provider: sso-provider
            client-id: SalesClientId
            client-name: Sales
            client-secret: password
            client-authentication-method: basic
            authorization-grant-type: authorization_code
            redirect-uri: '{baseUrl}/login/oauth2/code/{registrationId}'
            scope: user_info
        provider:
          sso-provider:
            authorization-uri: http://172.16.180.22/oauth/authorize
            token-uri: http://172.16.180.22/oauth/token
            jwkSetUri: http://172.16.180.22/.well-known/jwks.json
            user-info-uri: http://172.16.180.22/user/me
            user-name-attribute: username

nicico:
  oauth2:
    token:
      cookie:
        domain: localhost
  rest-api:
    url: http://localhost:9099

logging:
  level:
    com.nicico: debug

---

spring:
  profiles: prod
  security:
    oauth2:
      client:
        registration:
          sso-login:
            provider: sso-provider
            client-id: SalesClientId
            client-name: Sales
            client-secret: password
            client-authentication-method: basic
            authorization-grant-type: authorization_code
            redirect-uri: '{baseUrl}/login/oauth2/code/{registrationId}'
            scope: user_info
        provider:
          sso-provider:
            authorization-uri: http://172.16.180.22/oauth/authorize
            token-uri: http://172.16.180.22/oauth/token
            jwkSetUri: http://172.16.180.22/.well-known/jwks.json
            user-info-uri: http://172.16.180.22/user/me
            user-name-attribute: username

nicico:
  oauth2:
    token:
      cookie:
        domain: production.icico.net.ir
  rest-api:
    url: http://production.icico.net.ir:9099

logging:
  level:
    com.nicico: info